package game;

public class Marlin extends Objeto{
    
    public Marlin(int x, int y, int largura, int altura, int velocX, int velocY, boolean ativo) {
        super(x, y, largura, altura, velocX, velocY, ativo);
    }
    
}
